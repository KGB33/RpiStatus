
def test_docker():
    """
    When the `/docker` endpoint is called,
    Assert that a Json object with the expected keys is returned.
    """
    assert False

def test_ps():
    """
    When the `/docker/ps` endpoint is called,
    Assert that a Json object with the expected keys is returned.
    """
    assert False

def test_logs():
    """
    When the `/docker/logs/<container>` endpoint is called,
    Assert that a Json object with the expected keys is returned.
    """
    assert False
